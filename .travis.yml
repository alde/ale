language: go

go:
 - 1.11

before_install:
  - go get github.com/go-playground/overalls
  - go get github.com/mattn/goveralls
  - go get -v github.com/Masterminds/glide
  - glide install

script:
 - make vet
 - go test $(glide novendor)
 - make ale
#  - overalls -project=github.com/alde/ale -covermode=count
#  - goveralls -coverprofile=overalls.coverprofile -service travis-ci || true

# after_success:
#  - misc/docker.sh
